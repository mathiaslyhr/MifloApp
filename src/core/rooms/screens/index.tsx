/**
 * Shared room screens, used by every game. Join discovers a room's game type
 * from the backend; Lobby runs the right game via its registered room config.
 */
export {JoinScreen} from './JoinScreen';
export {LobbyScreen} from './LobbyScreen';
